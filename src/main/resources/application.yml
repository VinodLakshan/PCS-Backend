# Server properties
server:
  servlet:
    context-path: /pcs

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/pcsdb?createDatabaseIfNotExist=true
    password:
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: 'true'

